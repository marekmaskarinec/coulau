import (
	"../csv.um"
)

const size = 200

fn main() {
	inp := make([][]str, size)

	for i:=0; i < size; i++ {
		inp[i] = make([]str, size)
		for j:=0; j < size; j++ {
			inp[i][j] = ":]"
		}
	}

	csv.parse(csv.encode(inp))
}
